# Handbook

A small document with real structure, so a tree read, a file read and a syntax-highlighted
render all have something to show.

## Sections

- Purpose — exercise the repository reader end to end.
- Shape — headings, a list, and a fenced code block.
- Lifetime — disposable; a test run may rewrite this file.

## Example

```ts
export function greet(name: string): string {
  return `Hello, ${name}`;
}
```

## Notes

This file is part of a throwaway E2E fixture. See the repository README.

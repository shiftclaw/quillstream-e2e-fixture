# e2e/

A second folder, so the repository tree has more than one level to render.

The quillstream editor's file rail lists folders before files; a fixture with a single
folder could not tell "the rail renders folders" from "the rail renders whatever it was
given".
